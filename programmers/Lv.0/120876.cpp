#include <string>
#include <vector>
#include <algorithm>
using namespace std;

int solution(vector<vector<int>> lines) {
    int answer = 0;
    int arr[250]={0};
    sort(lines[0].begin(),lines[0].end());
    for(int i = 0; i<lines.size();i++){
        
    }


    return answer;
}